<%= turbo_frame_tag "swimlane_#{@swimlane.id}_new_card" do %>
  <%= form_with model: [:swimlane, @card] do |form| %>
    <%= form.label :title, "Title" %>
    <%= form.text_field :title, required: true %>
    <%= form.submit %>
    
    <i class="fa-solid fa-x"></i>
  <% end %>
<% end %>
